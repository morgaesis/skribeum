// The hardcoded document the M0 shell renders. The end-to-end smoke test
// asserts this exact string appears in the editor, so it is exported from
// one place and imported by both the app and the test.
export const M0_FIXTURE = `# Skribeum

Byte-faithful editing of *plain* Markdown, rendered by CodeMirror 6.

- [ ] scaffold fixture, replaced when vaults open
`;
