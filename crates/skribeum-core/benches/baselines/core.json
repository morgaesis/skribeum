{
  "schema": 1,
  "runner_class": "linux-x64",
  "window": 5,
  "threshold_percent": 10,
  "benchmarks": {
    "parse_extract/parse": {
      "samples_ns": [
        2722.9
      ]
    },
    "parse_extract/extract": {
      "samples_ns": [
        2412.2
      ]
    }
  },
  "note": "Samples come from continuous integration runs on the runner class named above. Locally recorded samples must never seed this file: this machine class differs from the runner class by several times, so a cross-machine comparison measures the host rather than the change."
}
