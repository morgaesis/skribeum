{
  "description": "The committed IPC command allowlist. CI fails when the tauri-specta generated bindings expose a command set different from this list. Additions are deliberate contract changes, never side effects.",
  "commands": [
    "vault_open",
    "vault_close",
    "vault_session_read",
    "vault_session_forget",
    "vault_session_clear_last",
    "vault_tree",
    "vault_tree_refresh",
    "note_create",
    "tree_folder_create",
    "tree_entry_move",
    "tree_entry_move_plan",
    "tree_entry_move_undo",
    "tree_entry_delete",
    "tree_entry_reveal",
    "note_read",
    "note_stat",
    "vault_file_read",
    "vault_file_write",
    "note_write",
    "edit_history_read",
    "edit_history_append",
    "edit_history_fence",
    "edit_history_clear",
    "watch_subscribe",
    "search_query",
    "tag_catalog",
    "update_check",
    "settings_read",
    "settings_path",
    "settings_write",
    "vault_config_read",
    "zoom_set",
    "window_warmup",
    "window_ready",
    "window_show_system_menu",
    "window_set_maximize_button_rect",
    "file_open_resolve",
    "open_files_take"
  ],
  "plugin_commands": ["plugin:dialog|confirm", "plugin:opener|open_url"],
  "notes": {
    "vault_session_read": "Reads the device-local startup-vault session document. Missing, corrupt, or unsupported documents produce an empty session.",
    "vault_session_forget": "Forgets one explicitly selected stale startup-vault candidate. Failed opens remain recorded until this command is called.",
    "vault_close": "Releases one native vault handle and stops its watcher and reconciliation workers. Repeating the command is harmless so superseded opens can clean up safely.",
    "vault_session_clear_last": "Clears only the authoritative automatic-startup selection while preserving the recent list; startup policy can still select one remaining recent vault.",
    "note_create": "Creates an empty Markdown note at a validated vault-relative path without overwriting an existing file, then refreshes the vault index.",
    "tree_folder_create": "Creates a folder at a validated vault-relative path and refreshes the vault and search indexes.",
    "tree_entry_move": "Moves or renames one indexed vault entry without replacing an existing entry, retargets every link that resolved to it so the vault's links keep resolving, then refreshes the vault and search indexes. The rewrite is recorded for tree_entry_move_undo.",
    "tree_entry_move_plan": "Reports the notes a move or rename would rewrite and how many links in each, so the operation can be described and declined before anything is written. Reads only.",
    "tree_entry_move_undo": "Reverses the last move or rename in a vault: the entry returns to its old path and every note rewritten with it is restored byte for byte.",
    "tree_entry_delete": "Deletes one indexed vault entry and refreshes the vault and search indexes.",
    "tree_entry_reveal": "Reveals one indexed vault entry in the operating system file manager.",
    "note_write": "Contract addition per design decision 100: change-set based writes verified against the expected projection hash, with a conflict variant carrying the current hash and a reconciliation handle, never a silent overwrite.",
    "edit_history_read": "Reads one note's reachable durable undo and redo stacks from application data.",
    "edit_history_append": "Appends and fsyncs an idempotent edit-history batch before the corresponding note save begins.",
    "edit_history_fence": "Appends an external-ingest boundary that makes older edit-history entries unreachable.",
    "edit_history_clear": "Physically removes one note's persisted edit-history journal after user confirmation.",
    "vault_tree_refresh": "Contract addition per design decision 100: on-demand re-index of the vault tree from current filesystem state, the recovery path for tree staleness after external bulk changes or watcher overflow.",
    "search_query": "Contract addition per design decision 100: ranked snippeted full-text search (the decision 79 alpha floor). The index lives in the OS app-data directory, never in the vault; match positions are byte ranges into the snippet so highlighting needs no HTML injection.",
    "tag_catalog": "Returns tags from the derived search index with note and occurrence counts for completion ranking.",
    "update_check": "Checks the selected stable or beta signed updater manifest and returns only current or available metadata. Installation remains in the updater plugin.",
    "settings_read": "Contract addition per design decision 100: reads settings.json from the OS app-config directory per the architecture contract (schema_version, typed known keys, defaults when absent).",
    "settings_path": "Returns the resolved settings document path for display in the desktop settings surface.",
    "settings_write": "Contract addition per design decision 100: whole-document settings write with validation; unknown keys already in the file are preserved internally.",
    "vault_config_read": "Contract addition per design decision 100: read-only access to the recognized Obsidian configuration files (app.json, types.json) in the vault's .obsidian directory, the single sanctioned read path into an otherwise excluded directory; absent or malformed files degrade to defaults.",
    "note_stat": "Reads one indexed note's filesystem creation and modification timestamps for the statusline's last-edited segment and note-info popover. Resolution goes through the vault index, so no unindexed path is ever statted.",
    "vault_file_read": "Read-only access to indexed regular files for registered render-only views. The command never creates note editing, reconciliation, or search-index state.",
    "vault_file_write": "Whole-document write for indexed non-note files. The canvas board is the only editable consumer; a card move, add, or remove rewrites the file through the crash-safe durable write sequence with no projection-hash conflict check, and never touches editor, reconciliation, or search-index state.",
    "zoom_set": "Validates, persists, and applies one application zoom percentage to every webview window.",
    "window_warmup": "Lets Linux WebKit commit an offscreen frame without exposing the startup window.",
    "window_ready": "Applies persisted zoom and reveals a hidden or warmed window after the frontend reports its first painted frame.",
    "window_show_system_menu": "Shows the window menu at the pointer for the header drag region's right-click: the real platform system menu on Windows (GetSystemMenu and TrackPopupMenu, forwarding the chosen command back through WM_SYSCOMMAND), a predefined-item approximation (Minimize, Maximize or Restore, Close) built fresh on every call elsewhere.",
    "window_set_maximize_button_rect": "Reports the Maximize caption button's current rectangle so Windows native hit-testing can answer WM_NCHITTEST for it and Windows 11 snap layouts appear on hover and hold; a no-op everywhere except Windows.",
    "file_open_resolve": "Resolves an operating-system file-open path to a known vault or the file's containing folder.",
    "open_files_take": "Drains file-open paths queued by argv, second-instance forwarding, or native open-file events.",
    "plugin:dialog|confirm": "Shows the destructive-action confirmation before clearing one note's edit history.",
    "plugin:opener|open_url": "Opens validated HTTP and HTTPS links in the system browser. The capability excludes paths and every other URL scheme."
  }
}
